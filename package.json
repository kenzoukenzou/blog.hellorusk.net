{
  "scripts": {
    "dev": "node posts-builder.js && next",
    "build": "next build",
    "export": "next export",
    "clean": "rm -rf .next out",
    "deploy": "npm run clean && node posts-builder.js && npm run build && npm run export",
    "blog": "node new-post-generator.js",
    "lint": "./node_modules/.bin/eslint . --fix"
  },
  "dependencies": {
    "@zeit/next-mdx": "^1.2.0",
    "next": "^8.0.0",
    "next-page-transitions": "^1.0.0-alpha.6",
    "react": "^16.8.1",
    "react-dom": "^16.8.1"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-plugin-react": "^7.12.4"
  }
}
